<Window x:Class="Soundboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Soundboard" Height="520" Width="640">
    <DockPanel Margin="12">

        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="Party output" Margin="0,0,0,4"/>
            <ComboBox x:Name="DeviceBox" Margin="0,0,0,8"/>
            <Button Content="Refresh clips" Click="Refresh_Click" Height="26"/>
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Margin="0,8,0,0">
            <TextBlock x:Name="HotkeyText" Foreground="#888" FontSize="11"
                       TextWrapping="Wrap" Margin="0,0,0,2"/>
            <TextBlock x:Name="StatusText" Foreground="Gray"/>
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ClipList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Label}"
                                Click="Clip_Click"
                                Width="140" Height="56" Margin="4"
                                FontSize="13"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </DockPanel>
</Window>